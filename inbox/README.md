# Inbox

* `inbox/` 用于临时放置未整理资料；
* 入口条件：资料暂时无法直接归入明确目录时，先进入 `inbox/`；
* `inbox/` 不是事实源，不作为长期归档。

## 出口机制

* 资料进入后应尽快归位到 `projects/` / `methods/` / `prompts/` / `personal-work/`；
* 项目事实类资料优先归入对应项目 `sources/`；
* 个人非项目资料归入 `personal-work/sources/`，必要时同步沉淀到 `personal-work/briefs/`；
* 不建议长期堆积未处理内容。
