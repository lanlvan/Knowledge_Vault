---
artifact_type: handoff_document_draft
page: open-box
display_name: open box
target_audience: 研发
output_mode: full
output_type: complete_handoff_document
version: "1.3"
status: draft
---

# Open Box 研发交接文档 v1.3 草稿

## 1. 交接范围

open box 页面用于承接用户开启军需的主流程。本版本覆盖左侧资源概览、中间猎户军需核心操作区、右侧卡牌背包、页面左下方「本期保底」运营活动卡牌，以及下方卡牌 / 饰品区中与保底相关的焦点区标记、保底奖励标记。

本页面的开启条件可由赠品次数、账号已有能量、卡牌填充能量及其他实际参与资源共同满足。中间区域负责展示本次开启判断与主操作，最终进度、状态与奖励结果以后端结算返回为准。

本版本不新增产品事实变化，只按当前已确认口径生成完整交接草稿。

## 2. 研发关注摘要

| 模块 | 研发关注点 |
| --- | --- |
| 中间核心操作区 | 开启次数、开启满足度、本次资源构成、主操作推荐态均以本次计入结果展示；前端不通过左侧资源与右侧选卡自行推导中间结果。 |
| 右侧卡牌背包 | 作为当前开箱流程的卡牌填充选择器，不是完整资产管理区；半展开态展示摘要，全展开态承载选卡操作与底部汇总。 |
| 能量溢出提示 | 仅在右侧全展开态底部固定汇总区展示「溢出 +Y% 存入能量余额」；不进入左侧资源概览、中间满足度、资源构成 icon 区或本期保底卡片。 |
| 本期保底 | 位于页面左下方，仅配置保底玩法的指定军需箱展示；不参与开启满足度、一键填充、开启军需按钮状态或资源构成 icon。 |
| 焦点区 / 保底标记 | 焦点区标记与保底奖励标记指向同一批卡牌，但含义不同；前端按后端配置展示，不自行合并或推导。 |

## 3. 页面结构

| 区域 | 位置 | 内容 | 边界 |
| --- | --- | --- | --- |
| 顶部导航 | 页面顶部 | BOMCASE Logo、导航菜单、用户相关入口、道具 / 消息 / 头像等入口 | 已有功能，本次不展开。 |
| 资源概览 | 页面左侧常驻面板 | 区域标题、副说明、资源概览 info、赠品开箱次数、能量余额、能量余额 info、即将过期能量提示、机械扳手、卡牌背包入口、会员等级 | 只做账号资源概览，不承担本次是否可开启判断，也不承担卡牌选择操作。 |
| 猎户军需核心操作区 | 页面中间主视觉与主操作区 | 页面标题、军需箱主视觉、X1 / X5、开启满足度、本次资源构成 icon、CTA 按钮区 | 当前页面主判断区和主操作区。 |
| 本期保底卡片 | 页面左下方区域 | 固定奖励视觉资源、模块标题、范围标签、info icon、进度文案、进度条、状态文案 | 仅展示当前军需箱保底状态与规则说明，不参与开启判断。 |
| 卡牌背包 | 页面右侧面板 | 半展开态摘要、全展开态卡牌选择、切换 icon、筛选、2 列卡牌列表、选中态、喜欢卡标识、轻提示、底部固定汇总区 | 当前开箱流程中的卡牌填充选择器，不是资产管理区。 |
| 下方饰品区域 | 页面下方 | 焦点区标记、保底奖励标记 | 仅说明两类标记与本期保底卡片的关联，其他已有规则不展开。 |
| 底部掉落信息 Tab | 页面底部 | 精彩掉落、最近掉落、个人掉落 | 已有功能，本次不展开。 |

## 4. 核心概念

| 概念 | 口径 |
| --- | --- |
| 焦点区卡牌 | 面向用户的保底命中范围表达。研发口径中，对应当前箱子内带保底标记的指定范围卡牌 / 饰品。 |
| 保底范围饰品 | 研发 / 后端理解口径，表示当前箱子中被配置为保底命中范围的指定卡牌 / 饰品。 |
| 焦点区标记 | 用于标识当前箱子中属于本期保底命中范围的卡牌。 |
| 保底奖励标记 | 用于标识触发保底后可能发放的奖励范围。 |
| 填充 +X% | 本次已选卡牌带来的新增能量，不包含账号已有能量。 |
| 卡牌能量 +X% | 本次开箱计入开启满足度的卡牌能量值，具体数值以后端返回为准。 |
| 溢出 +Y% | 本次已选卡牌能量超过当前开启所需补足能量后，开启后沉淀至账号能量余额的部分。 |

焦点区卡牌与保底范围饰品指向同一组配置对象，不是两个奖励池。前台主表达统一使用「焦点区卡牌」，研发 / 后端口径可使用「保底范围饰品」。

## 5. 字段与文案

### 5.1 左侧资源概览

| 字段 / 文案 | 说明 | 展示条件 / 边界 |
| --- | --- | --- |
| 资源概览 | 左侧区域标题。 | 常驻。 |
| 开启资源状态 | 左侧区域副说明。 | 常驻。 |
| 右上角 info | 资源概览模块说明入口。 | hover 展示说明。 |
| 0 | 赠品开箱次数示例；作为 L1 主资源展示。 | 可展示 0。 |
| + | 赠品开箱次数右侧加号入口。 | 已有功能，本次不展开。 |
| 18% | 账号已有能量余额示例；作为 L2 次主资源展示。 | 仅展示账号当前已有能量，不等同于右侧本次选卡新增能量。 |
| 能量余额 info | 能量余额说明入口。 | hover 展示说明。 |
| 即将过期 5% | 即将过期能量提示示例。 | 仅当存在即将过期能量时，在能量余额卡片内展示；正常状态不展示。 |
| 7 | 机械扳手数量示例。 | L3 辅助资源展示。 |
| 3张 | 用户当前拥有的卡牌总数示例。 | 左侧作为资产入口，不等同于右侧当前筛选后展示数量。 |
| › | 卡牌背包入口箭头。 | 点击后联动右侧卡牌背包。 |
| Lv.4 | 会员等级示例。 | 只展示身份信息。 |
| 2% | 会员等级进度示例。 | 具体升级口径为已有功能，本次不展开。 |

资源概览右上角 info 建议文案：

```text
展示账号当前与开启军需相关的资源概览，具体开启判断以中间区域为准。
```

能量余额 info 建议文案：

```text
能量余额为账号当前已有能量，可用于补足开启满足度；具体来源与有效期以能量明细为准。
```

即将过期能量 hover 文案：

```text
即将过期能量为近期可能失效的能量，请在有效期内使用。具体过期时间以系统记录为准。
```

左侧卡牌背包入口文案采用：

```text
卡牌背包
N张 ›
```

左侧会员等级仅展示：

```text
会员等级
Lv.N
X%
```

左侧不展示会员等级 info，不新增会员等级点击交互，不展示 VIP 权益加成值。

### 5.2 中间核心区

| 字段 / 文案 | 说明 | 展示条件 / 边界 |
| --- | --- | --- |
| 猎户军需 | 中间区域页面标题。 | 常驻。 |
| 军需箱图片 | 军需箱主视觉。 | 固定展示，不随资源状态变化。 |
| X1 / X5 | 开启次数选择。 | 当前选中项高亮，未选中项弱化但可点击。 |
| X1 | 当前选中的开启次数示例。 | 按用户选择展示。 |
| 开启满足度 | 进度条区域标题。 | 常驻。 |
| 23% / 68% / 78% / 100% | 当前开启满足度展示值示例。 | 示例值不作为固定统计口径。 |
| 进度条 | 当前满足度展示。 | 以后端返回结果展示。 |
| 还差 X%，可填充卡牌补足 | 进度条下方状态文案。 | 未满足且存在可填充卡牌，或已有选卡但仍不足时展示。 |
| 已满足开启条件 | 进度条下方状态文案。 | 已满足开启条件时展示。 |
| 赠品次数 ×N | 本次资源构成 icon 文案。 | 纯赠品或混合路径时展示；赠品未参与时不展示。 |
| 卡牌能量 +X% | 本次资源构成 icon 文案。 | 展示本次开箱计入满足度的卡牌能量值，具体数值以后端返回为准。 |
| 扳手 ×N | 本次资源构成 icon 文案。 | 能量开箱路径下展示。 |
| VIP权益 +X% | 权益徽章文案。 | 会员权益参与时展示，与资源 icon 做视觉区分。 |
| 一键填充 | 补足动作按钮。 | 用于自动选择卡牌填充能量。 |
| 开启军需 | 最终动作按钮。 | 用于执行开启。 |
| 开启军需始终可点击，条件不足时仅触发已有功能 | 底部说明文案。 | 条件不足时点击沿用已有功能。 |

进度条下方文案只保留四类：

| 状态 | 文案 |
| --- | --- |
| 未满足，存在可填充卡牌 | 还差 X%，可填充卡牌补足 |
| 已满足 | 已满足开启条件 |
| 未满足，无任何资源参与 | 无，不展示文案 |
| 未满足，已有选卡但仍不足 | 还差 X%，可填充卡牌补足 |

中间区域不展示完整账号资源总览、不展示完整卡牌列表、不展示本次卡牌填充溢出说明。

### 5.3 本期保底卡片

| 字段 / 文案 | 说明 | 展示条件 / 边界 |
| --- | --- | --- |
| 本期保底 | 保底模块前台标题。 | 仅配置保底玩法的指定军需箱展示。 |
| 焦点区卡牌 | 面向用户的保底命中范围表达。 | 研发口径对应当前箱子内带保底标记的指定范围卡牌 / 饰品。 |
| 固定奖励视觉资源 | 活动卡片表达资源。 | 不代表最终实际发放奖励，不根据实际奖励内容动态替换。 |
| info icon | 保底规则说明入口。 | hover 后展示保底规则说明。 |
| 进度 X/Y | 常规累计态文案。 | X 表示当前本轮已累计次数，Y 表示当前保底门槛；以后端返回为准。 |
| 再开启 N 次触发 | 临近完成态文案。 | N 以后端返回或后端计算结果为准。 |
| 已获得焦点区卡牌，新一轮已开启 | 自然命中焦点区态文案。 | 不额外触发保底奖励发放。 |
| 奖励发放中... | 结算触发态瞬时文案。 | 产品建议最多展示 2 秒；如后端结果返回较快，可不强制停满 2 秒。 |
| 上一轮已完成，新一轮已开启 | 新一轮延续态文案。 | 承接自然命中或触发保底后的新一轮。 |
| 分段进度条 | 进度条形态。 | 当 Y ≤ 10 时展示。 |
| 连续进度条 | 进度条形态。 | 当 Y > 10 时展示。 |

保底 info 建议内容：

```text
焦点区卡牌为本期保底范围。
未获得焦点区卡牌时，保底进度增加。
获得焦点区卡牌或触发保底后，本轮保底完成并开启新一轮。
触发保底后，将直接获得焦点区范围内的 1 个奖励。
```

### 5.4 右侧卡牌背包

| 字段 / 文案 | 说明 | 展示条件 / 边界 |
| --- | --- | --- |
| 卡牌背包 | 右侧区域标题。 | 半展开和全展开均展示。 |
| 选卡填充能量 | 全展开副标题。 | 仅全展开态展示。 |
| 共 N 张卡牌 | 用户当前拥有的卡牌总数。 | 半展开态展示，不等同于全展开列表数量。 |
| 全展开卡牌列表 | 当前可用于填充选择的卡牌数组。 | 展示数量可受筛选、可用状态影响。 |
| 赠品次数已满足，暂无需填充 | 半展开 / 全展开未选卡状态文案。 | 赠品次数已满足时展示。 |
| 开启能量已满足，暂无需填充 | 半展开 / 全展开未选卡状态文案。 | 开启能量已满足时展示。 |
| 已选 N 张，填充 +X% | 半展开填充态摘要。 | 已有卡牌被选中并计入当前填充结果时展示。 |
| 暂无可用卡牌 | 半展开无卡牌状态文案。 | 当前开箱流程中无可用于填充的卡牌时展示。 |
| 查看背包 | 半展开入口文案。 | 有卡牌且无需填充时展示。 |
| 查看背包调整 | 半展开入口文案。 | 有卡牌且已有填充时展示。 |
| 前往获取 | 半展开无卡牌入口文案。 | 点击打开 CTA 下方建议弹窗。 |
| 收起 / 展开切换 icon | 右侧半展开 / 全展开状态切换。 | 样式以最终设计稿为准。 |
| 能量由高到低 | 全展开筛选项。 | 已有功能，本次不展开。 |
| 全部等级 | 全展开筛选项。 | 已有功能，本次不展开。 |
| D / C / B / A | 卡牌稀有度或等级展示值。 | 已有功能，本次不展开。 |
| MP7 精锐版 | 卡牌名称示例。 | 长名称可截断展示。 |
| 武器图片 | 卡牌主视觉图片。 | 卡牌项内展示。 |
| +10% | 卡牌可填充能量值。 | 选中态与未选中态需有明确视觉区分。 |
| 喜欢卡 | 喜欢卡展示文案。 | 喜欢卡显示明确标识。 |
| 喜欢卡标识 | 喜欢卡标识。 | 具体 icon 样式以最终设计稿为准。 |
| 已选中 | 卡牌是否被选中。 | 选中后立即计入当前填充结果。 |
| ✓ | 卡牌选中勾选标识。 | 选中态展示。 |
| 喜欢卡已保护，未自动填充 | 一键填充跳过喜欢卡轻提示。 | 仅在用户触发一键填充且系统跳过喜欢卡时展示。 |
| 已选 N 张｜填充 +X%｜清除已选 | 全展开底部汇总区文案。 | 已选卡牌数量大于 0 时展示。 |
| 已选卡牌将在开启时消耗 | 全展开底部补充说明。 | 已选卡牌数量大于 0 且不存在能量溢出时展示。 |
| 已选卡牌将在开启时消耗，溢出 +Y% 存入能量余额 | 全展开底部补充说明。 | 已选卡牌数量大于 0 且本次填充存在能量溢出时展示。 |
| 已选 0 张｜选择卡牌后填充能量 | 全展开底部文案。 | 需要填充且未选卡时展示。 |

### 5.5 下方饰品区域

| 字段 / 标记 | 说明 | 边界 |
| --- | --- | --- |
| 焦点区标记 | 标记卡牌 / 饰品是否属于本期保底命中范围。 | 仅说明该标记与本期保底卡片有关，饰品区域其它功能本次不展开。 |
| 保底奖励标记 | 标记卡牌 / 饰品是否属于触发保底后的可能奖励范围。 | 仅说明该标记与本期保底卡片有关，饰品区域其它功能本次不展开。 |

## 6. 资源与能量规则

### 6.1 卡牌数量口径

左侧「卡牌背包 N张」与右侧半展开态「共 N 张卡牌」均表示用户当前拥有的卡牌总数。

右侧全展开列表是卡牌选择器，展示数量可受筛选、可用状态影响，不等同于用户拥有卡牌总数。喜欢卡保护不影响右侧列表展示，仅影响一键填充自动选择逻辑。

### 6.2 能量口径

| 位置 | 展示内容 | 口径 |
| --- | --- | --- |
| 左侧资源概览 | 能量余额 18% | 账号已有能量余额。 |
| 右侧底部汇总 / 半展开填充态 | 填充 +X% | 本次已选卡牌带来的新增能量，不包含账号已有能量。 |
| 右侧底部汇总补充说明 | 溢出 +Y% 存入能量余额 | 本次已选卡牌能量超过当前开启所需补足能量时，超出部分在开启后沉淀至账号能量余额；仅存在溢出时展示。 |
| 中间资源构成 icon | 卡牌能量 +X% | 本次开箱计入满足度的卡牌能量值，具体数值以后端返回为准。 |

前端不通过「左侧账号已有能量 + 右侧本次选卡新增能量」自行计算中间「卡牌能量」展示值。中间「开启满足度」与资源构成均以后端返回的本次开箱判断结果为准。

「溢出 +Y%」仅在存在溢出能量时展示；不存在溢出时，仍展示原文案「已选卡牌将在开启时消耗」。

本文档仅约束展示口径，不展开已有能量计算规则、实际扣减逻辑或资源消耗优先级。

### 6.3 资源参与口径

左侧资源概览展示账号资源总览；中间资源构成 icon 区只展示本次实际参与开启判断的资源。

| 路径 | 中间显示 icon | 中间不显示 |
| --- | --- | --- |
| 纯赠品路径 | 赠品次数 ×N | 卡牌能量、扳手、VIP |
| 纯能量路径 | 卡牌能量 +X%、扳手 ×N、VIP权益 +X%（如有） | 赠品 ×0 |
| 混合路径 | 赠品次数 ×N、卡牌能量 +X%、扳手 ×N、VIP权益 +X%（如有） | 未参与资源 |
| 都没有资源参与 | icon 区不显示任何内容 | 全部资源 icon |

当仅有 1 个资源 icon 时，使用完整资源名称文案，例如「赠品次数 ×1」。未参与资源不展示，不显示 ×0 或空占位。

### 6.4 X1 / X5 与赠品部分抵扣

X1 / X5 为开启次数选择。当选择 X5 且赠品次数不足 5 次时，赠品次数可计入本次开启满足度，剩余部分可通过卡牌能量等资源补足。

切换 X1 / X5 后：

- 已选卡牌清空；
- 开启满足度重新计算；
- 中间资源构成 icon 重新计算；
- 右侧状态重新判断；
- 右侧底部固定汇总区清空已选态与能量溢出提示；
- 本期保底卡片不因 X1 / X5 切换直接变化，保底进度仅在开启结算后按后端结果刷新。

## 7. 本期保底规则

本期保底卡片仅作为当前军需箱的保底状态展示，不参与本次开启条件判断。

| 项目 | 口径 |
| --- | --- |
| 是否参与开启满足度计算 | 不参与 |
| 是否影响一键填充逻辑 | 不影响 |
| 是否影响开启军需按钮状态 | 不影响 |
| 是否进入资源构成 icon 区 | 不进入 |
| 是否直接影响 X1 / X5 切换 | 不直接影响 |
| 是否影响右侧卡牌背包 | 不影响 |
| 是否影响卡牌填充能量 | 不影响 |
| 是否影响资源消耗优先级 | 不影响 |
| 数据来源 | 以后端配置与接口返回为准 |

| 类型 | 说明 |
| --- | --- |
| 自然命中焦点区 | 用户开启结果中获得任一焦点区卡牌，视为命中保底范围，本轮保底完成，进度重置，并进入新一轮，不额外触发保底奖励发放。 |
| 未命中焦点区 | 用户开启结果中未获得任一焦点区卡牌，本轮保底进度 +1。 |
| 触发保底 | 连续未命中焦点区达到保底门槛后，触发保底；奖励由后端按当前箱子的保底配置，在指定范围卡牌中随机发放 1 个奖励；本轮保底完成，进度重置，并进入新一轮。 |

保底奖励发放边界：

- 触发保底后不再发放军需箱，不再要求用户二次开启；
- 触发保底后的奖励在后端结算完成时即发放；
- 保底奖励发放后自动开启新一轮保底进度；
- 保底奖励发放不依赖前端结果页播放状态；
- 结果页仅作为告知展示，不作为奖励发放的触发节点。

批量开启边界：

- X5 等批量开启场景下，前端不逐次计算命中与进度变化；
- X5 等批量开启的保底最终展示以后端结算结果为准；
- 前端不自行推导跨轮保底结果；
- 批量开启中如同时发生自然命中焦点区、触发保底奖励、新一轮进度变化等结果，前端不自行判断展示优先级，最终结果态文案与新一轮进度以后端返回为准。

达到保底门槛后的展示说明：

- 达到保底门槛后的展示以最终结算结果为准；
- 不要求前台停留展示 `Y/Y` 过程态；
- 触发保底、自然命中焦点区等结果态用于本次开启后的结果反馈或刷新后的短期展示；
- 页面最终稳定态以结算后的新一轮保底进度为准；
- 结果态具体展示时长、消失方式、是否有过渡动效需后续确认。

## 8. 状态规则

### 8.1 左侧资源概览

| 层级 | 内容 | 规则 |
| --- | --- | --- |
| L1 主资源 | 赠品开箱次数 | 视觉权重需高于 L2、L3，保留加号入口。 |
| L2 次主资源 | 账号已有能量余额 | 展示总量，权重低于赠品次数，高于 L3。 |
| L3 辅助资源 / 入口 / 身份 | 机械扳手、卡牌背包、会员等级 | 弱于 L1 / L2，不抢主视觉。 |

能量余额默认只展示账号已有能量总量，例如「18%」，不常驻展示失效明细。仅当存在即将过期能量时，在能量余额卡片内展示「即将过期 X%」。

### 8.2 中间核心区域

军需箱主视觉固定展示，不随当前资源状态或 X1 / X5 切换变化。

X1 / X5 当前选中项高亮，未选中项弱化展示但保持可点击。切换后清空已选卡牌，并重新计算开启满足度。

资源构成 icon 区只展示本次实际参与资源：

- 赠品、卡牌能量、扳手属于资源 icon；
- VIP 权益属于权益徽章 icon，与资源 icon 做视觉区分；
- 赠品 ×0 不展示；
- 未参与资源不展示；
- 本期保底卡片不进入本次资源构成 icon 区；
- 本次能量溢出提示不进入本次资源构成 icon 区。

### 8.3 本期保底卡片

| 状态 | 触发条件 | 前台展示文案 | 展示说明 |
| --- | --- | --- | --- |
| 未配置保底 | 当前军需箱未配置保底玩法 | 不展示 | 模块不展示、不占位。 |
| 常规累计态 | 配置保底，且当前未临近触发 | 进度 X/Y | 正常累计中，按当前 X/Y 展示。 |
| 临近完成态 | 距离保底触发较近，例如剩余 N 次 | 再开启 N 次触发 | 用于强化即将达成感，N 以后端返回或后端计算结果为准。 |
| 自然命中焦点区态 | 本次开启结果获得任一焦点区卡牌 | 已获得焦点区卡牌，新一轮已开启 | 本轮保底完成并重置，不额外触发保底奖励发放。 |
| 结算触发态 | 连续未命中达到保底门槛，进入奖励发放过程 | 奖励发放中... | 瞬时态，产品建议最多展示 2 秒；如后端结果返回较快，可直接进入新一轮延续态。 |
| 新一轮延续态 | 上一轮已完成，新一轮已开启 | 上一轮已完成，新一轮已开启 | 承接自然命中或触发保底后的重置说明，展示新一轮初始进度。 |

前台不使用「重置 / 清零」作为主表达。常态保持轻表达，完整规则放入 info。

### 8.4 CTA 按钮区

按钮区固定展示：

```text
一键填充
开启军需
```

两个按钮位置固定常驻，不隐藏、不换位。

| 状态 | 一键填充 | 开启军需 | 说明 |
| --- | --- | --- | --- |
| 赠品足够 | 非推荐状态，视觉权重低于推荐操作；不可填充或非推荐状态仍需可识别。 | 推荐操作，用户应能识别当前推荐动作。 | 推荐直接开启。 |
| 能量足够 | 非推荐状态，视觉权重低于推荐操作；不可填充或非推荐状态仍需可识别。 | 推荐操作，用户应能识别当前推荐动作。 | 推荐直接开启。 |
| 可填充但未满足 | 推荐操作，用户应能识别当前推荐动作。 | 非推荐状态，视觉权重低于推荐操作。 | 推荐先填充。 |
| 都没有 | 非推荐状态，仍需可识别。 | 常规可点击按钮态，不作为推荐操作。 | 点击后提示获取资源或补足资源，沿用已有功能。 |

同一时刻只能有一个主要推荐操作。一键填充是补足动作，不是最终开箱动作；开启军需是最终动作。

### 8.5 右侧半展开态

半展开态定义为当前状态摘要。半展开态只承载当前状态摘要与进入全展开态的入口，不承载具体选卡操作。

半展开态不展示：

- 副标题「选卡填充能量」；
- 筛选项；
- 卡牌列表；
- 底部固定汇总；
- 能量溢出轻提示；
- `[0/3]`、`3/300` 等未确认计数。

有卡牌且赠品足够：

```text
卡牌背包

共 N 张卡牌
赠品次数已满足，暂无需填充

查看背包 ›
```

有卡牌且能量已满：

```text
卡牌背包

共 N 张卡牌
开启能量已满足，暂无需填充

查看背包 ›
```

有卡牌且已有填充：

```text
卡牌背包

共 N 张卡牌
已选 N 张，填充 +X%

查看背包调整 ›
```

无可用卡牌：

```text
卡牌背包

暂无可用卡牌

前往获取 ›
```

无卡牌状态指当前开箱流程中无可用于填充的卡牌，不等同于账号历史上没有任何卡牌。有卡牌但当前无需填充时，不使用空状态插图，不显示「暂无可用卡牌」。

### 8.6 右侧全展开态

全展开态定义为操作详情，展示结构为：

```text
卡牌背包
选卡填充能量

[能量由高到低] [全部等级]

2 列卡牌列表

动态轻提示

底部固定汇总区
```

顶部区域展示「卡牌背包」与「选卡填充能量」，保留半展开 / 全展开切换 icon，不展示 `[0/3]`、`3/300` 等未确认口径计数。

筛选区展示「能量由高到低」「全部等级」，不展示「清除」。「清除已选」仅在底部汇总区展示。

卡牌列表采用 2 列布局。每张卡展示稀有度 / 等级、卡牌名、卡牌图片、能量值、喜欢标识、选中勾选。全展开态仅卡牌列表区域可滚动；顶部标题 / 筛选区与底部固定汇总区不随列表滚动。

| 状态 | 视觉表现 | 说明 |
| --- | --- | --- |
| 普通卡 · 未选 | 未选中态正常展示，需与已选中态有明确视觉区分。 | 点击直接选中，无需确认。 |
| 普通卡 · 已选 | 展示已选中状态标识，需与未选中态有明确视觉区分。 | 再次点击反选撤回。 |
| 当前操作项 | 选中瞬间需有可识别反馈。 | 稳定后回到已选中态。 |
| 喜欢卡 · 未选 | 常驻喜欢卡标识，正常展示，不弱化。 | 点击触发二次确认。 |
| 喜欢卡 · 已选 | 同时表达喜欢保护与已选中状态。 | 已确认后正常计入填充结果，可反选。 |
| 一键填充跳过喜欢卡 | 喜欢卡不自动选中。 | 展示动态轻提示说明已跳过。 |

喜欢卡轻提示文案：

```text
喜欢卡已保护，未自动填充
```

该提示不是常驻信息。仅在用户触发一键填充后，系统跳过喜欢卡时出现；如果未发生一键填充跳过喜欢卡，不展示该提示。

底部固定汇总区：

| 场景 | 底部文案 |
| --- | --- |
| 需要补能量，未选卡 | 已选 0 张｜选择卡牌后填充能量 |
| 赠品已满足，未选卡 | 赠品次数已满足，暂无需填充 |
| 能量已满足，未选卡 | 开启能量已满足，暂无需填充 |
| 已有选择 | 已选 N 张｜填充 +X%｜清除已选 |
| 已有选择 · 无溢出补充说明 | 已选卡牌将在开启时消耗 |
| 已有选择 · 有溢出补充说明 | 已选卡牌将在开启时消耗，溢出 +Y% 存入能量余额 |

「清除已选」仅在已选卡牌数量大于 0 时展示，作为弱文字按钮，不抢主操作权重。

### 8.7 下方饰品保底相关标记

| 元素 | 规则 |
| --- | --- |
| 焦点区标记 | 用于标记该卡牌属于本期保底命中范围。 |
| 保底奖励标记 | 用于标记该卡牌属于触发保底后的可能奖励范围。 |
| 展示范围 | 按当前军需箱配置展示；两类标记指向同一批卡牌，但表达含义不同。 |
| 不适用卡牌 | 不属于对应配置范围的卡牌不展示对应标记。 |
| 合并边界 | 前端不自行合并、不自行推导、不用单一标记替代两类标记，除非产品和 UI 另行确认。 |
| 是否新增交互 | 不新增点击或 hover 交互。 |
| 其它饰品规则 | 已有功能，本次不展开。 |

## 9. 交互规则

### 9.1 左侧资源概览

| 触发对象 | 触发条件 | 结果 | 适用范围 | 不适用处理 |
| --- | --- | --- | --- | --- |
| 赠品开箱次数加号 | 点击 `+` | 已有功能，本次不展开。 | 已有功能。 | 已有功能。 |
| 资源概览右上角 info | hover | 展示资源概览说明。 | 适用。 | 无。 |
| 能量余额 info | hover | 展示能量余额说明。 | 适用。 | 无。 |
| 即将过期 X% | hover | 展示即将过期能量说明弹层，该弹层为后续迭代能力。 | 仅存在即将过期能量时展示。 | 无即将过期能量时不展示入口。 |
| 卡牌背包 N张 › | 点击 | 右侧卡牌背包进入全展开态。 | 右侧非全展开状态。 | 无。 |
| 卡牌背包 N张 › | 点击 | 如果右侧已为全展开态，则右侧区域做抖动或高亮等效果标识。 | 右侧已全展开状态。 | 具体采用抖动还是高亮需后续确认。 |

会员等级不新增 info，不新增点击交互。本次能量溢出提示不在左侧资源概览触发，不新增左侧点击或 hover 规则。

### 9.2 中间核心区域

| 触发对象 | 触发条件 | 点击结果 | 适用范围 | 不适用处理 |
| --- | --- | --- | --- | --- |
| X1 / X5 | 点击未选中的开启次数 | 切换当前开启次数，已选卡牌清空，开启满足度重新计算。 | X1 / X5 切换。 | 当前已选项重复点击不产生切换。 |
| 一键填充 | 存在可填充卡牌，且当前需要补能量 | 自动选择卡牌，更新右侧选中态与中间开启满足度。 | 可填充场景。 | 无。 |
| 一键填充 | 无可填充卡牌 | 一键填充不作为当前推荐操作，但状态仍需可识别。 | 无可填充场景。 | 不补充提示文案。 |
| 一键填充 | 一键填充过程中遇到喜欢卡 | 喜欢卡默认跳过，右侧展示轻提示「喜欢卡已保护，未自动填充」。 | 仅触发一键填充且存在喜欢卡时。 | 未跳过喜欢卡时不展示提示。 |
| 开启军需 | 开启条件满足 | 执行开启。 | 满足条件时。 | 无。 |
| 开启军需 | 开启条件不满足 | 触发已有功能，本次不展开。 | 不满足条件时。 | 已有功能，本次不展开。 |
| 保底 info icon | 当前军需箱配置保底玩法，且本期保底卡片展示 | hover 后展示保底规则说明。 | 仅本期保底卡片展示时。 | 未配置保底玩法时不展示模块，因此不展示该 icon。 |

中间区域不新增能量溢出 hover、弹窗或说明入口。

### 9.3 本期保底状态变化

| 项目 | 说明 |
| --- | --- |
| 触发对象 | 用户开启当前军需箱。 |
| 触发条件 | 当前军需箱配置保底玩法。 |
| 未命中焦点区结果 | 保底进度 +1。 |
| 自然命中焦点区结果 | 本轮保底完成并开启新一轮，不额外触发保底奖励发放。 |
| 达到保底门槛结果 | 后端按当前箱子的保底配置，在指定范围卡牌中随机发放 1 个奖励，并开启新一轮。 |
| 不适用处理 | 未配置保底玩法的军需箱不展示模块，不执行本模块展示逻辑。 |
| 前端展示 | 开启后展示最终结算后的保底状态与进度，具体以后端返回为准。 |
| 前端计算边界 | 前端不自行计算单次保底结果，开启后展示以后端返回结果为准。 |
| 结算触发态说明 | 结算触发态为瞬时态，产品建议最多展示 2 秒；如后端结果返回较快，可不强制停满 2 秒。 |

本期保底卡片本体、固定奖励视觉资源、进度条、焦点区标记、保底奖励标记均不新增点击规则。

### 9.4 批量开启结算

| 项目 | 说明 |
| --- | --- |
| 触发对象 | X5 等批量开启。 |
| 触发条件 | 当前军需箱配置保底玩法，且用户进行批量开启。 |
| 最终展示 | 批量开启结束后，模块展示后端返回的最终结算结果。 |
| 奖励发放 | 触发保底后，后端按当前箱子的保底配置，在指定范围卡牌中随机发放 1 个奖励；不发放军需箱，不要求用户二次开启。 |
| 结果页边界 | 结果页仅作为告知展示，不作为奖励发放的触发节点。 |
| 计算来源 | 最终展示以后端结算结果为准，前端不逐次计算命中与进度变化，不自行推导跨轮保底结果。 |
| 多结果场景 | 如同时发生自然命中焦点区、触发保底奖励、新一轮进度变化等结果，前端不自行判断展示优先级，最终结果态文案与新一轮进度以后端返回为准。 |

### 9.5 右侧半展开态

| 触发对象 | 触发条件 | 点击结果 | 适用范围 | 不适用处理 |
| --- | --- | --- | --- | --- |
| 查看背包 › | 半展开有卡牌，且当前无需填充 | 进入右侧全展开态。 | 有卡牌 · 赠品足够 / 有卡牌 · 能量已满。 | 无卡牌时不展示该文案。 |
| 查看背包调整 › | 半展开有卡牌，且已有填充 | 进入右侧全展开态，用于调整已选卡牌。 | 已有填充状态。 | 无已选卡牌时不展示。 |
| 前往获取 › | 半展开无可用卡牌 | 点击打开 CTA 下方建议弹窗。 | 无卡牌状态。 | 有卡牌状态不展示。 |
| 右侧顶部切换 icon | 点击 | 在半展开 / 全展开之间切换。 | 右侧卡牌背包区域。 | 具体视觉反馈按最终 UI 样式执行。 |

半展开态不展示「溢出 +Y% 存入能量余额」。如需查看溢出提示，用户需进入全展开态查看底部固定汇总区。

### 9.6 右侧全展开态

| 触发对象 | 触发条件 | 点击结果 | 适用范围 | 不适用处理 |
| --- | --- | --- | --- | --- |
| 普通卡卡片 | 普通卡未选中 | 选中卡牌，立即计入当前填充结果。 | 普通卡适用。 | 不可用卡牌状态未确认，不写规则。 |
| 已选卡牌卡片 | 已选状态 | 反选撤回，底部汇总回退。 | 已选卡牌适用。 | 无。 |
| 喜欢卡卡片 | 喜欢卡未选中 | 触发二次确认。 | 喜欢卡未选中适用。 | 取消确认则不选中。 |
| 喜欢卡二次确认 | 确认加入 | 卡牌进入选中态，计入当前填充结果。 | 喜欢卡二次确认适用。 | 无。 |
| 喜欢卡二次确认 | 取消 | 不选中卡牌。 | 喜欢卡二次确认适用。 | 无。 |
| 清除已选 | 已选卡牌数量 > 0 | 清除所有已选卡牌，底部回到对应未选状态。 | 已选状态展示。 | 未选状态不展示。 |
| 筛选：能量由高到低 | 点击筛选项 | 已有功能，本次不展开。 | 全展开态适用。 | 半展开态不展示。 |
| 筛选：全部等级 | 点击筛选项 | 已有功能，本次不展开。 | 全展开态适用。 | 半展开态不展示。 |

选择或反选卡牌后，底部汇总区同步更新「已选 N 张｜填充 +X%｜清除已选」。当选择结果存在能量溢出时，底部补充说明同步展示「已选卡牌将在开启时消耗，溢出 +Y% 存入能量余额」。当选择结果不存在能量溢出时，底部补充说明展示「已选卡牌将在开启时消耗」。

清除已选后，底部不再展示「已选卡牌将在开启时消耗」或「溢出 +Y% 存入能量余额」。

### 9.7 状态切换

| 触发 / 条件 | 切换结果 | 说明 |
| --- | --- | --- |
| 赠品次数已满足，且无已选卡牌 | 右侧默认摘要模式。 | 半展开展示「赠品次数已满足，暂无需填充」。 |
| 开启能量已满足，且无已选卡牌 | 右侧默认摘要模式。 | 半展开展示「开启能量已满足，暂无需填充」。 |
| 存在已选卡牌且进入半展开 | 展示半展开填充态。 | 半展开展示「已选 N 张，填充 +X%」，不展示溢出提示。 |
| 存在已选卡牌且进入全展开 | 展示全展开已选态。 | 底部展示「已选 N 张｜填充 +X%｜清除已选」；如存在能量溢出，补充展示「已选卡牌将在开启时消耗，溢出 +Y% 存入能量余额」。 |
| 系统判断用户可通过一键填充补足并开启当前最小开启次数 | 右侧默认全展开。 | 用于引导用户完成卡牌选择或一键填充。 |
| 其他非引导场景 | 右侧默认摘要模式。 | 不默认展开完整卡牌列表。 |
| 点击查看背包 / 查看背包调整 | 右侧全展开。 | 展示操作详情。 |
| 点击一键填充 | 自动选择可填充卡牌，并更新中间开启满足度与右侧选中结果。 | 仅当本次一键填充结果满足「可开启最少次数」的引导条件时，右侧自动进入全展开态并展示已选卡牌；其他场景保持或回到摘要态。 |
| 一键填充后存在能量溢出 | 右侧全展开态底部固定汇总区展示溢出提示。 | 展示「已选卡牌将在开启时消耗，溢出 +Y% 存入能量余额」。 |
| 一键填充跳过喜欢卡 | 右侧展示轻提示。 | 提示「喜欢卡已保护，未自动填充」。 |
| 切换 X1 / X5 | 已选卡牌清空，右侧和中间重新计算状态。 | 底部汇总区与能量溢出提示同步清空。 |
| 完成开启结算 | 本期保底卡片根据后端最终结算结果刷新。 | 仅配置保底玩法时适用。 |
| 离开页面 | 已选清空。 | 选卡是当前会话预览，不持久化。 |

## 10. 空状态

### 10.1 右侧卡牌背包无卡牌状态

| 项目 | 内容 |
| --- | --- |
| 触发条件 | 当前开箱流程中无可用于填充的卡牌。 |
| 展示文案 | 暂无可用卡牌 |
| 是否展示空状态插图 | 是，样式以最终设计稿为准。 |
| 是否展示按钮 | 是。 |
| 按钮文案 | 前往获取 |
| 点击结果 | 打开 CTA 下方建议弹窗。 |

该状态不等同于账号历史上没有任何卡牌。有卡牌但当前无需填充时，不使用空状态插图，不显示「暂无可用卡牌」。无卡牌状态不展示「已选卡牌将在开启时消耗」或「溢出 +Y% 存入能量余额」。

### 10.2 本期保底卡片空状态

本次未确认保底模块空状态。当前已确认处理为：

```text
未配置保底玩法的军需箱不展示本期保底卡片，不占位。
```

除此之外，不额外设计空状态。

### 10.3 能量溢出提示空状态

本次不单独设计能量溢出空状态。当前已确认处理为：

```text
不存在能量溢出时，不展示“溢出 +Y% 存入能量余额”。
```

除此之外，不额外设计空状态、占位、弹窗或说明入口。

## 11. 待后续确认边界

以下内容不阻塞当前研发启动，但未补充前不得按确定规则开发或验收。

| 编号 | 所属区域 | 待补充内容 | 影响范围 | 归属 |
| --- | --- | --- | --- | --- |
| 1 | 左侧资源概览 / 右侧卡牌背包 | 左侧「卡牌背包 N张 ›」点击后，如果右侧已为全展开态，具体采用「抖动」还是「高亮」效果，以及视觉持续时间。 | UI 状态、交互反馈验收。 | UI 主导，前端确认实现。 |
| 2 | 左侧资源概览 | 即将过期能量 hover 弹层的具体内容、字段、样式与触发延迟，作为后续迭代补充。 | UI 交互、前端实现、后续迭代验收。 | 产品主导，UI / 前端后续承接。 |
| 3 | 焦点区标记 / 保底奖励标记 | 两类标记的具体视觉样式。 | UI 视觉设计、前端资源。 | UI 主导，前端确认实现。 |
| 4 | 本期保底卡片 | 保底 info 的展示位置、宽度、样式与动效细节。 | UI 交互、前端实现。 | UI 主导，前端确认实现。 |
| 5 | 本期保底卡片 / 结果态 | 自然命中态 / 新一轮延续态的展示时长、消失方式、是否有过渡动效。 | UI 交互、前端实现、结果态验收。 | 产品确认展示规则，UI / 前端承接。 |

## 12. 不覆盖范围

- 不补完整资源扣减、库存、订单、支付、发货、资产、权益到账、结算、交易、钱包系统规则。
- 不把示例值写成固定系统规则。
- 不把待后续确认边界写成已确认产品事实。
- 不定义后端奖励随机算法。
- 不定义后端保底配置后台流程。
- 不定义奖励库存、资产到账、异常补偿、发放失败等后端规则。
- 不定义批量开启中每一次开箱的逐步进度动画。
- 不新增保底奖励领取按钮、弹窗或二次确认流程。
- 不新增保底奖励详情页。
- 不在左侧展示会员权益加成数值。
- 不在左侧会员等级上新增 info 或点击交互。
- 不把右侧卡牌背包做成资产管理区。
- 不在半展开态展示筛选、卡牌列表和底部固定汇总。
- 不在半展开态展示「溢出 +Y% 存入能量余额」。
- 不在右侧筛选区展示「清除」。
- 不在右侧顶部展示未确认口径的 `[0/3]`、`3/300` 等数量。
- 不在无确认情况下新增 loading、分页、权限、异常状态。
- 不重新定义资源消耗优先级、实际扣减逻辑、能量计算规则。
- 不因能量溢出提示新增独立能量沉淀模块、弹窗、领取按钮或结算前确认流程。
- 不把能量溢出提示放入中间开启满足度、资源构成 icon 区、本期保底卡片或左侧资源概览。
- 不新增累计返赠、任务、领取按钮、活动中心入口等未确认功能。
- 不使用「累计返赠」作为模块名称，模块前台标题统一为「本期保底」。
- 不展开资源概览、卡牌背包、底部掉落 Tab、CTA 按钮等已有功能规则。
- 不额外设计保底 loading、异常态、无权限态、按钮置灰态、未确认空状态。
- 不把本期保底卡片放回中间开启满足度区域。
- 不把固定设计资源当作实际发放奖励。
- 不把「焦点区卡牌」与「保底范围饰品」理解为两个奖励池。
- 不将「焦点区标记」与「保底奖励标记」混用。

## 13. 验收口径

### 13.1 页面结构验收

- 页面应包含左侧资源概览、中间核心操作区、右侧卡牌背包三个主要区域。
- 左侧资源概览区域应包含：赠品开箱次数、能量余额、机械扳手、卡牌背包入口、会员等级。
- 中间核心区域应包含：军需箱主视觉、X1 / X5、开启满足度、资源构成 icon、一键填充、开启军需。
- 本期保底卡片应位于页面左下方区域。
- 本期保底卡片应以运营活动卡牌形式表达。
- 仅配置保底玩法的指定军需箱展示本期保底卡片。
- 未配置保底玩法时，不展示本期保底卡片，不保留空位。
- 右侧卡牌背包区域应支持半展开态和全展开态。
- 半展开和全展开应使用同一组件骨架，不应表现为两个不同组件。
- 全展开态应使用 2 列卡牌布局。
- 全展开态仅卡牌列表区域可滚动；顶部标题 / 筛选区与底部固定汇总区不随列表滚动。
- 能量溢出提示仅归属右侧卡牌背包全展开态底部固定汇总区，不新增独立模块。
- 下方卡牌 / 饰品区需要支持展示焦点区标记。
- 下方卡牌 / 饰品区需要支持展示保底奖励标记。

### 13.2 字段展示验收

- 左侧赠品开箱次数展示为 L1 主资源，可展示 0。
- 左侧能量余额展示为 L2 次主资源，仅展示账号已有能量。
- 左侧即将过期提示保留在能量余额卡片内。
- 左侧机械扳手、卡牌背包、会员等级展示为 L3 信息。
- 左侧会员等级不展示 info，不新增点击交互。
- 左侧不展示会员权益加成数值。
- 左侧卡牌背包展示为「卡牌背包 N张 ›」形式。
- 左侧「N张」表示用户当前拥有卡牌总数。
- 右侧半展开「共 N 张卡牌」表示用户当前拥有卡牌总数。
- 右侧全展开列表展示数量可受筛选、可用状态影响，不等同于用户拥有卡牌总数。
- 喜欢卡保护不影响右侧列表展示，仅影响一键填充自动选择逻辑。
- 中间开启满足度应展示百分比大字和进度条。
- 中间资源构成 icon 只展示本次实际参与资源。
- 中间「卡牌能量 +X%」以后端返回的本次计入满足度的卡牌能量值为准，前端不自行根据左侧与右侧相加推导。
- 中间未参与资源不展示，不显示 ×0。
- 本期保底卡片应展示前台标题「本期保底」。
- 本期保底卡片应展示范围标签「焦点区卡牌」。
- 常规累计态展示轻文案「进度 X/Y」。
- 临近完成态展示「再开启 N 次触发」。
- 自然命中焦点区态展示「已获得焦点区卡牌，新一轮已开启」。
- 结算触发瞬时态展示「奖励发放中...」。
- 新一轮延续态展示「上一轮已完成，新一轮已开启」。
- 本期保底卡片应展示 info icon。
- Y ≤ 10 时展示分段进度条，Y > 10 时展示连续进度条。
- 固定奖励视觉资源仅用于活动卡片表达，不代表最终实际发放奖励。
- 保底结果态文案应以后端返回为准展示，不由前端自行推导。
- 右侧顶部展示收起 / 展开切换 icon，不展示 `[0/3]`、`3/300` 等未确认口径计数。
- 右侧半展开填充态必须展示「已选 N 张，填充 +X%」。
- 右侧半展开态不展示「溢出 +Y% 存入能量余额」。
- 右侧全展开已选态必须展示「已选 N 张｜填充 +X%｜清除已选」。
- 右侧全展开已有选择时，底部固定汇总区应展示「已选卡牌将在开启时消耗」。
- 当本次已选卡牌存在能量溢出时，右侧全展开底部固定汇总区应展示「已选卡牌将在开启时消耗，溢出 +Y% 存入能量余额」。
- 当不存在能量溢出时，不展示「溢出 +Y% 存入能量余额」。
- 「溢出 +Y%」表示本次开启消耗后沉淀至账号能量余额的部分。
- 「溢出 +Y%」不应展示在中间开启满足度、资源构成 icon 区或左侧资源概览中。
- 右侧「填充 +X%」表示本次已选卡牌带来的新增能量，不包含左侧账号已有能量。
- 卡牌能量值选中态与未选中态需有明确视觉区分，具体颜色以最终设计稿为准。
- 下方卡牌 / 饰品区应按后端配置展示焦点区标记与保底奖励标记。

### 13.3 状态验收

- 有卡牌且赠品次数满足时，右侧半展开展示「赠品次数已满足，暂无需填充」。
- 有卡牌且开启能量已满时，右侧半展开展示「开启能量已满足，暂无需填充」。
- 有卡牌且已有填充时，右侧半展开展示「已选 N 张，填充 +X%」。
- 无可用卡牌时，右侧半展开展示「暂无可用卡牌」和「前往获取」。
- 无可用卡牌时，右侧半展开应展示空状态插图。
- 一键填充跳过喜欢卡时，右侧展示轻提示「喜欢卡已保护，未自动填充」。
- 非一键填充跳过喜欢卡场景，不展示该提示。
- 喜欢卡提示不是默认常驻说明。
- 可填充但未满足时，一键填充为推荐态。
- 已满足开启条件时，开启军需为推荐态。
- 都没有资源时，中间资源构成 icon 区不展示任何内容。
- 都没有资源时，中间进度条下方不展示文案。
- 无可填充卡牌时，一键填充不作为当前推荐操作，但状态仍需可识别。
- 当选择 X5 且赠品次数不足 5 次时，赠品次数可计入本次开启满足度，剩余部分可通过卡牌能量等资源补足。
- X1 / X5 切换后，开启满足度和资源构成 icon 应重新计算。
- X1 / X5 切换后，已选卡牌清空，右侧底部固定汇总区不再展示已选态、消耗说明或能量溢出提示。
- 已选卡牌且不存在能量溢出时，右侧全展开态底部展示「已选卡牌将在开启时消耗」。
- 已选卡牌且存在能量溢出时，右侧全展开态底部展示「已选卡牌将在开启时消耗，溢出 +Y% 存入能量余额」。
- 清除已选后，右侧全展开态底部不展示「已选卡牌将在开启时消耗」或「溢出 +Y% 存入能量余额」。
- 未配置保底玩法时，不展示本期保底卡片。
- 常规累计态、临近完成态、自然命中焦点区态、结算触发态、新一轮延续态均需可识别。
- 本次开启未获得任一焦点区卡牌时，本轮保底进度 +1。
- 本次开启获得任一焦点区卡牌时，本轮保底完成，进度重置，并进入新一轮。
- 自然命中焦点区态不触发额外保底奖励。
- 连续未命中焦点区达到保底门槛时，由后端按当前箱子的保底配置，在指定范围卡牌中随机发放 1 个奖励。
- 触发保底后，本轮保底完成，进度重置，并进入新一轮。
- 批量开启时，保底最终展示以后端返回结果为准。
- 批量开启中如同时发生自然命中焦点区、触发保底奖励、新一轮进度变化等结果，前端不自行判断展示优先级。
- 结算触发态仅作为瞬时态，不作为长期稳定态。
- 结算触发态产品建议最多展示 2 秒；如后端结果返回较快，可不强制停满 2 秒。
- 新一轮开启后，展示后端返回的新一轮进度。

### 13.4 交互验收

- info 类统一 hover 触发。
- 左侧「卡牌背包 N张 ›」点击后，右侧应进入全展开态。
- 当右侧已处于全展开态时，再点击左侧「卡牌背包 N张 ›」，右侧应通过抖动或高亮等方式进行区域提示，具体方式待确认。
- X1 / X5 切换后，已选卡牌应清空，并重新计算开启满足度。
- X1 / X5 切换后，能量溢出提示应同步清空。
- 右侧「前往获取 ›」点击打开 CTA 下方建议弹窗。
- 普通卡点击后直接选中，并立即计入当前填充结果。
- 已选普通卡再次点击后反选撤回。
- 喜欢卡未选中时点击应触发二次确认，已有功能，本次不展开。
- 喜欢卡确认加入后正常计入填充结果。
- 一键填充时应跳过喜欢卡。
- 一键填充跳过喜欢卡时，右侧应展示轻提示。
- 一键填充后如存在能量溢出，右侧全展开态底部固定汇总区应展示溢出提示。
- 清除已选后，右侧已选状态清空。
- 清除已选后，右侧底部不再展示能量溢出提示。
- 开启条件满足时，点击开启军需执行开启。
- 开启条件不满足时，点击开启军需触发已有功能，本次不展开。
- 离开页面后，已选卡牌不持久化。
- 离开页面后，能量溢出提示不持久化。
- 即将过期能量提示 hover 后展示说明弹层，该弹层为后续迭代能力。
- 保底 info icon hover 后展示保底规则说明。
- 本期保底卡片本体不新增点击规则。
- 固定奖励视觉资源不新增点击规则。
- 进度条不新增点击规则。
- 焦点区标记不新增点击规则。
- 保底奖励标记不新增点击规则。

### 13.5 保底 info 验收

保底 info 至少包含以下说明：

```text
焦点区卡牌为本期保底范围。
未获得焦点区卡牌时，保底进度增加。
获得焦点区卡牌或触发保底后，本轮保底完成并开启新一轮。
触发保底后，将直接获得焦点区范围内的 1 个奖励。
```

info 内容用于前台规则说明，不替代后端实际结算规则。实际保底进度、门槛、奖励、范围以后端配置与接口返回为准。

### 13.6 标记验收

- 焦点区标记用于命中判断范围识别。
- 保底奖励标记用于触发保底后的奖励范围识别。
- 焦点区标记与保底奖励标记指向同一批卡牌，但表达含义不同。
- 前端不得将焦点区标记与保底奖励标记强行合并为同一标记。
- 前端不得只展示其中一个标记来替代另一个标记，除非产品和 UI 另行确认。
- 前端不得根据卡牌位置、稀有度、图片或名称自行判断是否属于焦点区或保底奖励范围。
- 标记展示以后端配置为准。

### 13.7 范围控制验收

- 不在左侧展示会员权益加成数值。
- 不在左侧会员等级上新增 info 或点击交互。
- 不把右侧卡牌背包做成资产管理区。
- 不在半展开态展示筛选、卡牌列表和底部固定汇总。
- 不在半展开态展示「溢出 +Y% 存入能量余额」。
- 不在右侧筛选区展示「清除」。
- 不在右侧顶部展示未确认口径的 `[0/3]`、`3/300` 等数量。
- 不在无确认情况下新增 loading、分页、权限、异常状态。
- 不重新定义资源消耗优先级、实际扣减逻辑、能量计算规则。
- 不因能量溢出提示新增独立能量沉淀模块、弹窗、领取按钮或结算前确认流程。
- 不把能量溢出提示放入中间开启满足度、资源构成 icon 区、本期保底卡片或左侧资源概览。
- 不新增累计返赠、任务、领取按钮、活动中心入口等未确认功能。
- 不使用「累计返赠」作为模块标题，模块前台标题为「本期保底」。
- 不展开资源概览、卡牌背包、底部掉落 Tab、CTA 按钮等已有功能规则。
- 不额外设计保底 loading、异常态、无权限态、按钮置灰态、未确认空状态。
- 保底常态保持轻表达，规则说明放入 info。
- 本期保底卡片不放回中间开启满足度区域。
- 本期保底卡片不参与开启满足度计算，不影响一键填充、开启军需按钮状态。
- 本期保底卡片不进入中间资源构成 icon 区。
- 不新增保底奖励领取按钮。
- 不新增保底奖励二次开箱流程。
- 不展示「获得军需箱后再开启」的旧规则表达。
- 不把固定设计资源当作实际发放奖励。
- 不把「焦点区卡牌」与「保底范围饰品」理解为两个奖励池。
- 不将「焦点区标记」与「保底奖励标记」混用。
- 能量溢出轻提示不影响一键填充、开启军需按钮状态。
- 对已有功能但本次未说明的内容，不展开流程和规则。
